# Public base URL of the site. Used for canonical URLs, sitemap and Open Graph tags.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Contact / reservation form delivery (leave blank until a provider is chosen).
CONTACT_INBOX_EMAIL=""

# Analytics (optional).
NEXT_PUBLIC_ANALYTICS_ID=""
